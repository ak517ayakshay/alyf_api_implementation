"""
Internal Relay Controllers

This module contains FastAPI route handlers for relaying requests to other services.
It defines endpoints for proxying requests to other services.

Endpoints:
    * /api/relay/{service}/{path:path}: relay_request
""" 