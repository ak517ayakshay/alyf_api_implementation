"""
Member Authentication Services

This module contains business logic for member authentication operations.
It handles member login and password reset functionality.

Functions:
    login_member(email, password)
    login_member_by_code(code)
    reset_member_password(email)
    validate_member_token(token)
""" 